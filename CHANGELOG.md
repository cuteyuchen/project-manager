# 更新日志

## Unreleased

## v1.7.0

### 新功能与优化

- Project Explorer 支持多级项目工作区、父子项目钻取，以及独立的排序、置顶和收藏；项目文件与 Markdown 备忘录可以在同一工作区连续管理。
- Git 管理增加仓库中心，统一覆盖贮藏、标签、远程与分支，并完善文件级 / hunk 级操作、提交历史搜索和历史操作。
- Node Runtime Center 统一管理应用内 Node Runtime，兼容扫描 NVM Runtime，支持系统 Node 切换、项目 Runtime 绑定和自定义 Node 路径。
- Run Session 明确呈现 Starting、Running、Success、Failed、Stopped，补充 exit code、耗时、停止、重新运行和当前 Runtime 信息。
- Run History 保留轻量运行结果，可按项目或命令检索失败原因、耗时和执行环境，并按当前配置重新运行。
- Console 支持 stdout、stderr、Runner 筛选、搜索、复制和导出；长日志使用容量限制与渲染缓冲，保持交互响应。
- 统一界面字号、间距和控件尺寸，优化设置分区导航、Git 布局记忆、亮暗色主题和自定义背景体验。

### 数据安全与生命周期

- Tauri 配置继续使用同目录临时文件、写入同步和替换的原子写入模型。
- `data.json` 增加单份 `data.json.bak` Last Known Good 备份；每次成功保存后滚动为“主文件 + 上一次成功备份”，迁移或规范化重写前先保护旧数据。
- uTools / ZTools 改用同目录临时文件、安全替换、失败回滚和临时文件清理，不再直接覆盖主配置；配置文件名统一限制为 basename，阻止路径穿越。
- 主配置损坏时进入只读保护；存在有效备份时支持用户确认后保留损坏快照、恢复备份并重新加载，主文件和备份都损坏时不影响应用进入只读模式。
- `run-history.json` 使用独立安全写入，损坏时回退为空，不影响 `data.json`。
- 退出、关闭到托盘和更新安装流程统一先 flush 项目数据与运行历史；只有真正退出且保存完成或用户明确选择继续退出后才清理运行中的进程。

### 发布工程与跨平台

- 发布流程拆分为 `release:preflight`、`release:prepare` 和 `release:publish`；版本更新、Cargo.lock、构建、测试、commit、tag、push、GitHub Release 或插件发布任一步失败都会以非零状态退出。
- 增加版本源一致性、tag 匹配、CHANGELOG release notes 提取、现有 tag 检查和 `--dry-run` 验证，避免构建失败后仍推送 tag 或伪造发布成功。
- Release notes 从 `CHANGELOG.md` 对应版本段落生成，避免 release workflow 使用过期的硬编码版本正文。
- uTools 构建改用跨平台 Node 文件复制脚本；统一 TypeScript 测试入口，并将 uTools / ZTools plugin build 纳入 CI gate。
- 普通 CI 增加 macOS smoke，持续覆盖 Windows、Ubuntu、macOS、前端构建、TypeScript 测试和 Rust 测试；正式发布仍保留 macOS Intel 与 Apple Silicon 构建矩阵。

## v1.6.2

### 新功能与优化

- 桌面端更新迁移到 Tauri 官方 updater：发布资产使用 minisign 签名，客户端会先验证签名再安装。
- Windows、macOS、Ubuntu deb 与 AppImage 自动匹配各自的安装器；Ubuntu deb 通过系统认证执行覆盖升级，AppImage 原位更新并重启。
- 更新下载过程支持后台运行，增加下载、验证、安装状态；安装前强制完成待保存数据，保存失败时可重试或取消更新。
- 新建远程项目不再使用单分支克隆，分支列表可显示全部远端分支；旧版单分支克隆的项目执行“获取”时会自动恢复完整远端分支引用。
- Ubuntu deb 补齐作者、MIT 许可证、主页、项目简介、软件分类、关键词和 AppStream 元数据，软件中心可显示项目图标及完整详情。

### 升级说明

- `v1.6.1` 的 Ubuntu deb 用户首次迁移到新更新器时，请执行 `sudo apt install ./Project.Manager_1.6.2_amd64.deb`；之后可直接使用应用内更新。

## v1.6.1

### 优化

- 桌面端更新下载进度改为固定在窗口右下角的 Element Plus 浮层，支持后台下载与取消下载，展示更稳定、操作更清晰。

### 修复

- 修复 Linux AppImage、deb 安装包和 macOS 应用包在重启后项目列表为空的问题：桌面端数据改为保存到系统用户数据目录，并自动迁移旧的可执行文件旁 `data.json`。
- 配置文件改为原子写入；读取或解析失败时进入只读保护，避免空项目状态覆盖原有数据。
- 修复退出应用时未等待最后一次项目数据保存完成的问题；保存失败时可重试或明确选择继续退出。

## v1.6.0

### 新功能

- 新增 Maven / Gradle Java 项目识别，支持 Wrapper，并自动提供构建、运行和测试命令。
- 子项目支持拖拽排序、置顶和收藏，父项目可聚合显示后代项目的运行状态。
- AI 提交信息生成支持单渠道多模型或多渠道回退，失败时按顺序尝试备用槽位。
- 新增聚焦搜索、新建项目、刷新项目和 Ctrl+1~5 侧边菜单快捷键，并支持在设置页配置。
- 设置页新增固定分区目录，支持点击跳转、滚动高亮和窄屏横向导航。

### 优化

- 按项目和层级记忆工作区当前子项目、页签和滚动位置，项目结构变化时自动修正失效记忆。
- Git Diff、提交历史和选中状态按项目分桶，切换项目不会串用其他项目的数据。
- 一级项目与子项目统一使用置顶优先、置顶顺序和普通排序规则。
- 设置页右侧内容限制在标题栏以下的剩余高度内独立滚动，左侧目录保持固定，底部内容可完整访问。

### 修复

- 修复切换子项目或一级项目后页签、滚动位置和选中状态丢失的问题。
- 修复子项目置顶按钮无效、父项目无法显示后代运行状态的问题。
- 修复不同项目之间 Git Diff 内容互相覆盖的问题。
- 修复 Ctrl+1~5 快捷键未生效，以及弹窗和输入控件场景下快捷键误触发的问题。

## v1.5.7

### 新功能

- 新增仓库中心，统一管理贮藏、标签、远程仓库与本地分支。
- 无暂存文件时提交会自动暂存全部变更；支持 amend 上次提交，以及 soft / mixed 撤销上次提交。
- 摘要识别冲突与进行中的 merge / rebase / cherry-pick / revert；冲突时禁止提交，合并中可继续或中止。
- 干净工作区时按 ahead / behind 突出「推送 N / 拉取 N」，双向分叉时给出提示；pull 支持 ff-only，push 支持 force-with-lease。
- 提交历史支持按描述 / 作者 / hash / 分支搜索；右键可复制、建分支、cherry-pick、revert、reset。
- 提交日历支持切换到当前月之前的历史月份，不能进入未来月；刷新保持当前查看月。

### 优化

- 统一 Git 模块工具栏、文件状态、Diff、历史图与提交区的视觉风格。
- 文件状态左侧栏默认 50% 宽度，按百分比持久化，窗口缩放自动适配。
- Git 较长错误可展开查看原始输出。
- 打通 stash / merge / rebase / tags / amend / reset / cherry-pick / revert，桌面端与插件端 API 同步。

### 修复

- 修复分支、仓库设置、仓库中心弹窗只在 Git 面板内居中的问题，现已相对整个窗口居中。

## v1.5.3

### 新功能

- 新增全局快速搜索，可统一检索项目名称、路径、描述、标签、`package.json` 脚本和自定义命令，并直接进入项目或定位待运行命令。
- 中文项目名和命令支持拼音全拼与首字母匹配。
- Tauri 桌面端新增独立快速搜索窗口，应用处于后台或最小化状态时也能通过系统级快捷键呼出。
- 设置页支持分别配置应用内搜索快捷键和系统级搜索快捷键，并提供组合键录制、标准化与校验。

### 优化

- 无搜索词时优先展示置顶项目和最近使用项目，使快速搜索窗口可以直接作为项目启动器使用。
- 搜索结果支持方向键切换、Enter 确认和 Escape 关闭。
- 独立搜索窗口每次显示时重新加载项目数据，失焦后自动隐藏，并兼容窗口拖动期间的焦点变化。
- 更新 README 功能说明、最新版界面截图和项目介绍文章。

## v1.5.2

### 优化

- 为 Tauri 桌面端预置独立快速搜索窗口及对应权限配置，支持置顶、隐藏任务栏、无边框和按需显示。
- 同步更新桌面端、Rust crate、uTools 与 ZTools 插件版本信息。

## v1.5.1

### 新功能

- 新增多级项目工作区，根项目可关联扫描到的子项目，并在侧栏中选择子项目执行各自的脚本、自定义命令和 Git 操作。
- 批量导入与手动导入统一支持自动扫描目录中的子项目；Git 克隆完成后也会重新扫描并挂载识别到的子项目。
- 当根项目包含子项目且根目录本身是 Git 仓库时，子项目列表新增“根目录（Git）”虚拟入口，可直接查看根仓库状态并执行根级命令，不产生重复项目数据。
- uTools 与 ZTools 插件补齐子项目扫描和批量导入预扫描能力，与 Tauri 桌面端保持一致。
- 设置页新增全局自定义背景图片，支持本地图片选择、预览、清除和背景显示强度调节，桌面端与插件端均可使用。
- 父子项目钻取新增方向动画；返回父项目或主项目列表时恢复离开前的滚动位置。

### 优化

- 批量导入、手动导入和项目内扫描统一复用同一套子项目类型、脚本和包管理器转换逻辑。
- 优化子项目卡片布局，使收藏图标与项目名称保持视觉对齐。
- 自定义背景启用后，主工作区和侧栏自动使用半透明模糊层，兼顾背景展示与内容可读性。
- 优化项目列表、Node、端口、提交日历等页面在自定义背景下的透明层级，减少大面积不透明区域。
- 功能页切换由串行淡出淡入改为交叠式位移淡化，减少切换过程中的空白和停顿感。
- 所有弹窗统一增加背景高斯模糊与强化遮罩，减少自定义背景和列表内容透出造成的重影。
- 统一主要页面的玻璃模糊强度和最大内容宽度，并优化宽屏与小屏下的数据面板布局。
- 项目列表、Node、端口、提交日历和设置页统一使用相同的页面标题、操作区、间距与顶部玻璃层结构。
- 提交日历的跳过项目默认折叠显示，项目名称过长时省略，可按需展开全部内容。
- 父子项目的文件和备忘录改为分别保存与展示；无可运行命令或环境配置时隐藏对应功能入口。
- 包含子项目的项目列表始终显示同级“父项目”入口，可随时切回父项目自身的功能与数据。
- 所有导入入口在添加根项目及子项目后统一扫描前端环境变量和 Vite 代理配置。

### 修复

- 修复未初始化 Git 的子项目被误识别为父级 Git 仓库的问题；现在只有项目目录本身等于仓库根目录时才显示 Git 状态。

## v1.4.0

### 优化

- 新增全局主题变量与通用页面、面板、按钮、弹窗样式，统一主工作台、设置、Node、端口、Git、项目文件、备忘录等区域的视觉风格。
- 收敛组件内硬编码颜色和重复暗色分支，改为语义化 token 驱动，提升亮色 / 暗色模式的一致性。
- 统一 Git 工具栏、提交区、文件管理工具栏、右键菜单、预览弹窗、设置卡片与项目列表的圆角、阴影、间距和交互反馈。
- 补充全局 focus-visible、reduced-motion 与滚动条样式，文件管理组件模板 DOM ref 改用 `useTemplateRef`。

### 文档

- README 增加界面体验说明，并同步本次版本更新内容。
